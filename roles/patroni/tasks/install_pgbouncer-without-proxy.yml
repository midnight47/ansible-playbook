# ---
# # Install PgBouncer on both RedHat and Debian/Ubuntu systems without using
# # a proxy.  For RedHat we enable the PGDG repository and then install
# # `pgbouncer`.  For Debian the package is available via the PGDG apt
# # repository or distribution repositories.

# - name: install PGDG repository for PgBouncer on RedHat
#   yum:
#     name: https://download.postgresql.org/pub/repos/yum/pgdg-redhat-repo-latest.noarch.rpm
#     state: present
#   when: ansible_os_family == 'RedHat'

# - name: Install PgBouncer on RedHat
#   yum:
#     name: pgbouncer
#     state: present
#   when: ansible_os_family == 'RedHat'

# - name: Install PgBouncer on Debian/Ubuntu
#   apt:
#     name: pgbouncer
#     state: present
#     update_cache: yes
#   when: ansible_os_family == 'Debian'

