---
# Install PgBouncer on both RedHat and Debian/Ubuntu systems through a proxy.
# For RedHat we enable the PGDG repository if needed and then install
# `pgbouncer`.  For Debian the package is available via the PGDG apt
# repository or distribution repositories.

# - name: install PGDG repository for PgBouncer on RedHat through proxy "{{ http_proxy }}"
#   yum:
#     name: https://download.postgresql.org/pub/repos/yum/pgdg-redhat-repo-latest.noarch.rpm
#     state: present
#   environment:
#     http_proxy: "{{ http_proxy }}"
#     https_proxy: "{{ https_proxy }}"
#   when: ansible_os_family == 'RedHat'

# - name: Install PgBouncer on RedHat through proxy "{{ http_proxy }}"
#   yum:
#     name: pgbouncer
#     state: present
#   environment:
#     http_proxy: "{{ http_proxy }}"
#     https_proxy: "{{ https_proxy }}"
#   when: ansible_os_family == 'RedHat'

# - name: Install PgBouncer on Debian/Ubuntu through proxy "{{ http_proxy }}"
#   apt:
#     name: pgbouncer
#     state: present
#     update_cache: yes
#   environment:
#     http_proxy: "{{ http_proxy }}"
#     https_proxy: "{{ https_proxy }}"
#   when: ansible_os_family == 'Debian'