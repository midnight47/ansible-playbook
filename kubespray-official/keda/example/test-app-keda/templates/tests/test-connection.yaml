apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test-app-keda.fullname" . }}-test-connection"
  labels:
    {{- include "test-app-keda.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "test-app-keda.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
