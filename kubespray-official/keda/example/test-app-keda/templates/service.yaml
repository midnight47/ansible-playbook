apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-app-keda.fullname" . }}
  labels:
    {{- include "test-app-keda.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-app-keda.selectorLabels" . | nindent 4 }}
